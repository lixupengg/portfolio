import * as React from 'react';
import { SectionHeader } from '@stonksfi/components';
import { StyledPageHeader } from './style';

interface Props {}

const DashboardMeta = (props?: Props) => {
	return (
		<>
			<SectionHeader
				label={'US Equities'}
			/>
		</>
	);
};

export default DashboardMeta;
